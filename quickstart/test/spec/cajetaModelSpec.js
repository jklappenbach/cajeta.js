define(
    ['cajeta'],
    function(Cajeta) {
        // First test classes and extend functionality
        return describe('Cajeta Model Tests', function() {
            it('instantiates', function() {
            });

            it('stores data', function() {
            });

            it('retrieves data', function() {
            });

            it('removes data', function() {

            });

            it('stores snapshots', function() {
            });

            it('restores snapshots', function() {
            });

            it('binds data to components', function() {
            });
        });
    }
);
